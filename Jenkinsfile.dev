pipeline {
    agent any

    stages {
        stage('Clonar Repositorio') {
            steps {
                checkout scm
            }
        }

        stage('Validar Maven') {
            steps {
                echo '${MAVEN}'
            }
        }
    }

    post {
        always {
            cleanWs()
        }
    }
}
